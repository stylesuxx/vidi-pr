You are consolidating multiple per-chunk review outputs into one final review for this pull request.

Each chunk produced its own four-section review of part of the diff. Your job: combine them into a single review with the same four sections (`## Summary`, `## Findings`, `## Suggestions`, `## Positives`), applying all the same output rules (valid GitHub-flavored Markdown, professional tone, no emojis, no em-dashes).

Deduplicate observations. Prioritize the most important findings first. Keep the tone unified. When two chunks contradict each other on the same point, prefer the more specific or better-supported observation and drop the other.

<chunk_outputs>

{{chunk_outputs}}

</chunk_outputs>
